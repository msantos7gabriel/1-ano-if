#include <stdio.h>

int main()
{

    int n, v[n];

    printf("Digite o numero de vetores: ");
    scanf("%i", &n);

    for (int i = 0; i < n; i++)
    {
        scanf("%i", &v[i]);
    }

    for (int i = 0; i < n; i++)
    {
        if (v[i] = i)
        {
            
        }
        
    }
    
}